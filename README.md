MirAI Recorder v1.1.1 (Chromium + Firefox ESR 128.11.0)
- Popup REC (rond rouge), status + chrono, loader mirai_loader.gif
- Trap URL, bookmarks visibles (★/☆), 10 dernières conférences
- Web Crypto (PBKDF2 + AES-GCM) pour chiffrer les identifiants via master password
- Options Vault (endpoint/token) + placeholder appel depuis background

Dev install:
- Chromium: chrome://extensions → Developer mode → Load unpacked
- Firefox ESR: about:debugging#/runtime/this-firefox → Load Temporary Add-on → manifest.json
